# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Variables

set $mod Mod4
set $editor x-terminal-emulator -e vim
set $fileman pcmanfm-qt
set $htop x-terminal-emulator -e htop
set $locker i3lock -c '#000000'
set $mailman thunderbird
set $musicplayer x-terminal-emulator -e ncmpcpp
set $scrotter flameshot
set $terminal x-terminal-emulator

# Some things to execute on i3 start
exec_always --no-startup-id hsetroot -cover '/home/glpz/.wallpaper.jpg'
exec_always --no-startup-id killall nm-tray; exec nm-tray &

exec --no-startup-id kdeconnect-indicator & 
exec --no-startup-id pcmanfm-qt -d
exec --no-startup-id xautolock -time 5 -locker $locker

# Default configurations

for_window [class="^.*"] border pixel 1
hide_edge_borders smart
# smart_gaps true
default_orientation horizontal
workspace_layout default
font pango:OpenSans Bold 8
floating_modifier mod1

# Default configurations for some apps:

## Assign workspaces to certain outputs (even number workspaces to HDMI)

workspace 1 output 	LVDS-1
workspace 2 output 	HDMI-1
workspace 3 output 	LVDS-1
workspace 4 output 	HDMI-1
workspace 5 output 	LVDS-1
workspace 6 output 	HDMI-1
workspace 7 output 	LVDS-1
workspace 8 output 	HDMI-1
workspace 9 output 	LVDS-1
workspace 10 output 	HDMI-1

assign [class="eDEX-UI"] → 10

## Apps that should be treated always as tile
for_window [title="Welcome to PhpStorm"] floating disable
for_window [class="pavucontrol-qt"] floating disable
for_window [class="lxqt-config-*"] floating disable

# Keyboard bindings

## My applications
# bindcode --release 133 exec rofi -show drun -theme dmenu.rasi
bindcode --release 133 exec rofi -show drun

bindsym $mod+e exec $fileman
bindsym $mod+l exec $locker
bindsym $mod+p exec x-terminal-emulator -e pulsemixer
bindsym $mod+Escape exec lxqt-leave
bindsym $mod+Return exec $terminal

bindsym Ctrl+Shift+Escape exec $htop
bindsym Ctrl+Escape exec lxqt-config

bindsym $mod+F1 exec zeal
bindsym $mod+F10 exec /home/glpz/Softlib/Telegram/Telegram

bindsym XF86Tools exec $musicplayer
bindsym XF86HomePage exec $fileman
bindsym XF86Mail exec $mailman
bindsym XF86Calculator exec kcalc

bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -10%
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +10%

### Taking screenshots
bindsym Print exec flameshot full
bindsym Shift+Print exec flameshot gui

### For keyboard indicators
bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Num_Lock exec pkill -SIGRTMIN+11 i3blocks

### Kill focused window
bindsym Mod1+F4 kill

## Manage i3

### Focus windows
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym Mod1+Tab focus right
bindsym Mod1+Shift+Tab focus left

### Move windows
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

### Split in horizontal orientation
bindsym $mod+h split h

### Split in vertical orientation
bindsym $mod+v split v

### Enter fullscreen mode for the focused container
bindsym F11 fullscreen toggle

bindsym $mod+Shift+space floating toggle

### Focus the parent container
bindsym $mod+a focus parent

### Focus the child container
bindsym $mod+d focus child

### Switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

### Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

### Reload the configuration file
bindsym $mod+Shift+c reload
### Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
### Exit i3 (logs you out of your X session)
bindsym $mod+q exec lxqt-leave 
### Reboot pc
bindsym Ctrl+Mod1+Shift+r exec reboot
### Shutdown pc
bindsym Ctrl+Mod1+Shift+h exec "lxqt-sudo shutdown -h now"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bar {
 	i3bar_command i3bar 
	position bottom
# 	height 0
 	status_command slstatus
 	output LVDS-1
 	tray_output LVDS-1
 	font pango:FontAwesome, Fira Code Retina 7
 	colors {
 		statusline #3ff0f1FF
# 		background #383b45ff
 		background #000000ff
 		focused_workspace #383b45ff #383b45ff #ffffffFF
 		inactive_workspace #000000FF #000000FF #222222FF
 	}
}

client.unfocused 	#383b45 #383b45 #eff0f1 #383b45 #383b45
client.focused_inactive #383b45 #383b45 #eff0f1 #383b45 #383b45
client.focused 		#eff0f1 #eff0f1 #383b45 #eff0f1 #333333
